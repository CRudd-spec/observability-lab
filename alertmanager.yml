route:
  receiver: 'default'
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1m

receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://172.18.0.1:5001/alert'
        send_resolved: true
